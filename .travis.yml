matrix:
  include:
    - sudo: required
      language: generic
      services:
        - docker
      before_install:
        - docker pull jpsim/sourcekit
      script:
        - docker run -v `pwd`:/SourceKitten jpsim/sourcekit bash -c "cd /SourceKitten && swift build && swift test"
branches:
  only:
    - master
notifications:
  email: false
  slack: realmio:vPdpsG9NLDo2DNlbqtcMAQuE
